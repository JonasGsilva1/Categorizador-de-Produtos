# === Supabase / PostgreSQL ===
# Use a URL do POOLER (porta 6543, modo Transaction) em Settings > Database > Connection string
DATABASE_URL=postgresql://postgres.xxxx:password@aws-0-us-east-1.pooler.supabase.com:6543/postgres

# === Supabase Auth (JWT Secret — Settings > API > JWT Secret) ===
# Para HS256: use o JWT Secret do Supabase
# Para RS256: use SUPABASE_URL abaixo (JWKS é buscado automaticamente)
SUPABASE_JWT_SECRET=sua-jwt-secret-do-supabase

# === Supabase URL (necessário para RS256/JWKS) ===
SUPABASE_URL=https://seu-projeto.supabase.co

# === Gemini (Google AI) ===
GEMINI_API_KEY=sua-chave-gemini

# === Tunning do Funil ===
SIMILARITY_THRESHOLD=0.98
LLM_CONFIDENCE_THRESHOLD=95
EMBEDDING_BATCH_SIZE=50

# === Frontend Origin (CORS) ===
# Produção: URL exata do Vercel (ex: https://categorizador.vercel.app)
FRONTEND_URL=https://seu-app.vercel.app

# === Server (Railway injeta PORT automaticamente) ===
PORT=8000
